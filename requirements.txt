# Core
python-dotenv>=1.0
pandas>=2.0
requests>=2.31

# PDF reading and generation
pdfplumber>=0.11
reportlab>=4.0

# Geospatial (EUBUCCO building footprints, read parquet + reproject WKB centroids)
pyarrow>=15.0
shapely>=2.0
pyproj>=3.6

# LLM providers (online). Mock mode needs none of these.
anthropic>=0.40
openai>=1.40
mistralai>=1.0

# RAG: LlamaIndex for indexing + retrieval, local HuggingFace embeddings.
# Generation stays on our own get_llm() wrapper; Settings.llm is forced to None
# so LlamaIndex never calls an online LLM. The embedding model runs locally.
llama-index-core>=0.14,<0.15
llama-index-embeddings-huggingface>=0.6,<0.8
sentence-transformers>=3.0

# UI (Streamlit MVP on main)
streamlit>=1.38

# Web backend (FastAPI) and client report generation
fastapi>=0.109
uvicorn>=0.27
python-multipart>=0.0.9
openpyxl>=3.1

# Tests
pytest>=8.0
